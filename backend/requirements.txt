fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
pydantic==2.10.4
pydantic-settings==2.7.0
openai==1.109.1
httpx==0.28.1
python-dotenv==1.0.1
python-multipart==0.0.20
azure-storage-blob==12.30.0
aiohttp==3.14.3  # async transport required by azure-storage-blob's aio client
pytest==8.3.4
pytest-asyncio==0.25.0

# Used by app/routers/experimental_deepagents.py (prototype chat path
# alongside, not instead of, the production Agent.stream() path) and
# app/routers/experimental_agent_groups.py (the Agent Groups feature).
# langchain-openai is pinned to the last release still compatible with
# openai's 1.x line above - langchain-openai>=1.2.0 requires openai>=2.x,
# which would be a breaking change for the production AsyncAzureOpenAI
# client shared by non-experimental chat.
deepagents==0.7.5
langchain-openai==1.1.9
